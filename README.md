# MultiPDFChat
### This is a MultiPDFChat application built using RAG and Groq API and deployed locally on streamlit UI.
### How to run?
#### Create Groq API key and incorporate it into the code
#### Create environment variable
bash python -m venv env
#### Install the required libraries
bash pip install -r requirements.txt
#### Run the app
bash streamlit run app.py
